<!-- 手机产品数据已经到了前端 -->
<view wx:for="{{phone_list}}" wx:for-item="phone"
 data-id="{{phone._id}}"
 bondtap="viewItem">
  <van-card 
  thumb="{{phone.image}}"
  title="{{phone.title}}"
  price="{{phone.price}}"
  >

  </van-card>
</view>